name: test

channels:
  - conda-forge
  
dependencies:
  # Base depends
  - python
  - pip

  # Specific dependencies
  - numpy
  - openpyxl
  - pandas
  - seamm
  - tabulate

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    # Integration testing
    - custom-step
    - loop-step
    
    # Documentation
    - rinohtype
    - pygments
    - sphinx-rtd-theme
    - pystemmer
